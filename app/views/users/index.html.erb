<h1>Your to-do list, sir</h1>

<header>
  <table>
    <thead>
      <tr>
        <th>List of To-Do</th>
      </tr>
    </thead>
  </table>
</header>

<tbody>
  <% @users.each do |x| %>
    <%= link_to x.name, user_path(x) %><br>
  <% end %> 
  <form action="/users/show" >
    <input type="submit" value="Add to-do" />
  </form>
</tbody>
</div>